{
  "name": "@football-app/sync-worker",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "tsx src/local.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@football-app/data-provider": "workspace:*",
    "@football-app/database": "workspace:*"
  },
  "devDependencies": {
    "@football-app/config": "workspace:*",
    "@types/aws-lambda": "^8.10.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
